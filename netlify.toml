[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Netlify detects Next.js and installs its own runtime plugin; nothing else to add.
# Set CONTACT_WEBHOOK_URL in Site configuration → Environment variables to deliver
# contact form submissions.
