[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Netlify detects Next.js and installs its own runtime plugin; nothing else to add.
# The contact form uses Netlify Forms — the form is declared in public/__forms.html
# and submissions appear under Site configuration → Forms.
